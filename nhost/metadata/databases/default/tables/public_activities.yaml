table:
  name: activities
  schema: public
insert_permissions:
  - role: user
    permission:
      check:
        user_id:
          _eq: X-Hasura-User-Id
      set:
        user_id: x-hasura-user-id
      columns:
        - import_id
        - import_file_id
        - source
        - external_id
        - activity_type
        - started_at
        - ended_at
        - duration_s
        - duration_kind
        - distance_m
        - elevation_gain_m
        - elevation_loss_m
        - avg_pace_s_per_km
        - avg_heart_rate_bpm
        - max_heart_rate_bpm
        - avg_cadence
        - calories_kcal
        - training_load
        - perceived_effort
        - notes
        - provider_payload
select_permissions:
  - role: user
    permission:
      columns:
        - user_id
        - id
        - import_id
        - import_file_id
        - source
        - external_id
        - activity_type
        - started_at
        - ended_at
        - duration_s
        - duration_kind
        - distance_m
        - elevation_gain_m
        - elevation_loss_m
        - avg_pace_s_per_km
        - avg_heart_rate_bpm
        - max_heart_rate_bpm
        - avg_cadence
        - calories_kcal
        - training_load
        - perceived_effort
        - notes
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      allow_aggregations: false
update_permissions:
  - role: user
    permission:
      columns:
        - notes
        - perceived_effort
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      check:
        user_id:
          _eq: X-Hasura-User-Id
delete_permissions:
  - role: user
    permission:
      filter:
        user_id:
          _eq: X-Hasura-User-Id
